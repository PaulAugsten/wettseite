## Summary

<!-- What does this change do, and why? -->

## Test plan

<!-- How did you verify this? e.g. unit tests added, manually tested locally, checked the preview deploy -->

- [ ] `pnpm lint && pnpm typecheck && pnpm test` pass locally
- [ ] Verified on the Vercel preview deploy (if UI-facing)

## Checklist

- [ ] No secrets, credentials, or `.env` values committed
- [ ] Breaking changes called out above (env vars, migrations, API contracts)
